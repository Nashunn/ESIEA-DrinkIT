<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/itemLayout"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_marginRight="2dip"
    android:padding="6dip"
    tools:layout_marginBottom="5dip"
    tools:layout_marginLeft="2dip"
    tools:layout_marginTop="5dip"
    >

    <ImageView
        android:id="@+id/cocktailIcon"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="6dip"
        android:contentDescription="Cocktail representation"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/tv_name"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_alignWithParentIfMissing="true"
        android:layout_toRightOf="@id/cocktailIcon"
        android:gravity="center_vertical"
        android:text="Unknown"
        android:textSize="16sp" />

    <ImageView
        android:id="@+id/iv_alcoholIndicator"
        android:layout_width="10dp"
        android:layout_height="26dp"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/tv_name"
        android:layout_marginBottom="15dp"
        android:tint="@color/colorPrimary"
        app:srcCompat="@drawable/ic_round_shape" />

    <TextView
        android:id="@+id/tv_isAlcoholic"
        android:layout_width="wrap_content"
        android:layout_height="22dp"
        android:layout_alignParentBottom="true"
        android:layout_toEndOf="@+id/iv_alcoholIndicator"
        android:text="Description"
        android:layout_marginStart="8dp"
        android:textSize="12sp" />

</RelativeLayout>